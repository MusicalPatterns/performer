// tslint:disable no-magic-numbers

import { Ms, to } from '@musical-patterns/utilities'

const PERFORMER_QA_PATTERN_TOTAL_DURATION: Ms = to.Ms(188000)

export {
    PERFORMER_QA_PATTERN_TOTAL_DURATION,
}
